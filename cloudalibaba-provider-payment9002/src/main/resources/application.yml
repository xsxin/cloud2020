server:
  port: 9002
spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
# 设置监控端点
management:
  endpoints:
    web:
      exposure:
        include: "*"
